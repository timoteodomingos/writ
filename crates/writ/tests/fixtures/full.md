# Heading 1

This is a regular paragraph with some **bold text** and some *italic text*. We can also have `inline code` and even ***bold and italic together***.

## Heading 2

Here's a paragraph with [a link](https://example.com) and some ~~strikethrough text~~.

### Heading 3

Testing **nested *formatting* inside** bold text.

#### Heading 4

A paragraph with *italic **and bold** together* to test complex nesting.

##### Heading 5

Text with `code` and **bold** and *italic* all in one line.

###### Heading 6

The smallest heading level.

## Lists

Here's a bullet list:

- First item
- Second item with **bold**
- Third item with *italic*
- Fourth item with `code`

And a numbered list:

1. First numbered item
2. Second numbered item with **formatting**
3. Third numbered item with [a link](https://rust-lang.org)

## Code Blocks

Here's a code block without language:

```
fn main() {
    println!("Hello, world!");
}
```

And one with language specified:

```rust
fn main() {
    println!("Hello from Rust!");
}
```

```javascript
function hello() {
    console.log("Hello from JavaScript!");
}
```

## Quotes

> This is a blockquote. It can span multiple lines.

> A quote with **bold text** and *italic text*.

## Complex Formatting

This paragraph has **bold with `code inside`** and *italic with **bold inside** too*.

Testing multiple styles: **bold**, *italic*, `code`, ~~strikethrough~~, and [links](https://github.com).

**Bold at the start** of a line.

*Italic at the start* of a line.

`Code at the start` of a line.

Text with **bold**, then normal, then *italic*, then normal again.

## Edge Cases

Empty lines between paragraphs should create separate blocks.

This paragraph has **bold** and the **bold continues** on the same line.

Text with *multiple* *italic* *sections*.

Testing **bold *and italic* and bold** again.

A [link with **bold text**](https://example.com) inside it.

## Nested Lists

- Item 1
  - Nested item 1.1
  - Nested item 1.2
- Item 2
  - Nested item 2.1

1. Numbered item 1
   1. Nested numbered 1.1
   2. Nested numbered 1.2
2. Numbered item 2

## Mixed Content

Here's everything together:

- A list item with **bold**, *italic*, `code`, and a [link](https://example.com)
- Another item with ~~strikethrough~~ and ***bold italic***

> A quote with **all** the *formatting* `types` and a [link](https://example.com)

Final paragraph with **bold *nested italic* bold** and *italic **nested bold** italic*.
